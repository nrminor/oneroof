# absolute path on remote host where files should be watched for
watch_path: /~

# glob pattern to use for finding particular files. Supported values are *.pod5,
# *.fastq.gz, and *.bam
pattern: "*.fastq.gz"

# host IP address
host: redacted

# username and password for an account on the remote host
username: redacted
password: redacted
