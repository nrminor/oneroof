[project]
name = "30252"
version = "0.1.0"
description = ""
authors = [{name = "Nicholas Minor", email = "nrminor@wisc.edu"}]
requires-python = ">= 3.10"
dependencies = ["ruff", "uv", "ipykernel", "pyright", "quarto", "polars", "pandas", "xlsxwriter", "xlsx2csv", "pre-commit", "just", "biopython", "pysam"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.pixi.project]
channels = ["conda-forge", "bioconda", "anaconda"]
platforms = ["osx-64", "linux-64"]

[tool.pixi.pypi-dependencies]
30252 = { path = ".", editable = true }

[tool.pixi.tasks]

[tool.pixi.dependencies]
samtools = ">=1.5,<2"
nextflow = ">=24.4.2,<24.5"
medaka = ">=1.11.3,<1.12"
minimap2 = ">=2.28,<3"
bbmap = ">=39.1,<40"
fastqc = ">=0.12.1,<0.13"
multiqc = ">=1.19,<2"
bcftools = ">=1.16,<2"
snakemake = ">=7.32.4,<7.33"
chopper = ">=0.8.0,<0.9"
pilon = ">=1.24,<2"
racon = ">=1.5.0,<1.6"
seqkit = ">=2.8.2,<2.9"
